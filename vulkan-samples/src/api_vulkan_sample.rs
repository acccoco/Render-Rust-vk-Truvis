pub struct ApiVulkanSample {}

impl ApiVulkanSample
{
    /// * 准备工作提交的 frame
    /// * 从 swapchain acquire 下一个 image
    /// * 设置默认的 wait 和 signal semaphore
    pub fn prepare_frame()
    {
        todo!()
    }
}
