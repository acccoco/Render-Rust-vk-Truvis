#pragma once
#include "bindless.slangi"
#include "common.slangi"

namespace blit
{
struct PushConstant
{
    ImageHandle src_image; // rt render
    ImageHandle dst_image; // final image to present

    uint2 src_image_size;
    uint2 offset;
};
};
