#pragma once
#include "common.slangi"

struct PBRMaterial
{
    float3 base_color;
    float metallic;

    float3 emissive;
    float roughness;

    uint diffuse_map;
    uint normal_map;
    uint _padding_1;
    uint _padding_2;
};

struct MatData
{
    uint4 mat_count;
    PBRMaterial materials[256];
};
